# =========================
# NEXT.JS FRONTEND (PROD)
# =========================

# ❗ НЕ ВКАЗУЄМО backend напряму
# Усі запити йдуть через /api/proxy
NEXT_PUBLIC_API_BASE=

# Debug (можеш вимкнути пізніше)
NEXT_PUBLIC_API_DEBUG=1

# Telegram bot username (без @)
NEXT_PUBLIC_TG_BOT_USERNAME=KyrhanuBot

# WebSocket (якщо реально використовується)
# Якщо WS через той самий фронт — можна лишити порожнім
NEXT_PUBLIC_WS_BASE=

# =========================
# INTERNAL / OPTIONAL
# =========================

# НЕ ПОВИННО використовуватись у клієнті
# Якщо це випадково потрапило у фронт — краще прибрати
# ADMIN_SECRET=  ❌ (не треба у фронті)